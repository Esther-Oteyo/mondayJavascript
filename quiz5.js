const arr = [1,2,3];
function doubleArray(numbers) {
    numbers.forEach(function(numbers) {
console.log(numbers * 2);
    
    });
    // numbers.forEach((numbers) => console.log(number * 2));
}
doubleArray(arr)
